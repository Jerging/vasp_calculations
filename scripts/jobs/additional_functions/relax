# VASP relax jobscript additional function

# Run relax setup
bash ~/scripts/structure/relax/setup.sh original_directory

cd relax

# Run initial relaxation
bash ~/scripts/structure/relax/repeat_relax.sh
